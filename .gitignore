# =============================================================================
# test-skill - Claude Code Audit Plugin
# =============================================================================
# Project-specific ignores + Universal Claude Code template
# =============================================================================

# -----------------------------------------------------------------------------
# TEST-SKILL SPECIFIC
# -----------------------------------------------------------------------------

# Sandbox directories created during Phase A/M testing
/sandbox/
/test-sandbox/
/mock-env/

# Audit output files (generated during /test runs)
audit-*.log
security-issues.json
quality-issues.json
*-issues.log
app-test-issues.log
production-issues.log

# CodeQL databases (large, generated)
codeql-db/
*.sarif

# Phase execution artifacts
phase-*.output
.phase-state/

# Local plugin settings (keep example)
*.local.md
!examples/*.local.md

# =============================================================================
# Claude Code - Universal .gitignore Template
# =============================================================================
# Security-critical patterns that should NEVER be tracked in any repository.
# =============================================================================

# -----------------------------------------------------------------------------
# SECURITY & CREDENTIALS (CRITICAL - Never track these)
# -----------------------------------------------------------------------------

# Environment variables (often contain secrets)
.env
.env.*
!.env.example
!.env.template

# Private keys and certificates
*.pem
*.key
*.crt
*.p12
*.pfx
*.jks

# Credentials and secrets
credentials.json
secrets.json
secrets/
.secrets/
*_credentials.json
*_secret.json
*.secret

# API keys and tokens
api_key*
apikey*
*_api_key*
*.token
auth_token*
access_token*

# OAuth and authentication
authcode.txt
*.authcode
oauth_tokens*
.oauth/

# SSH keys (should never be in repos)
id_rsa
id_rsa.pub
id_ed25519
id_ed25519.pub
*.ppk

# GPG keys
*.gpg
*.asc
secring.*

# Password files
*.password
*.passwd
passwords.txt
htpasswd

# AWS credentials
.aws/
aws_credentials
aws_config

# Cloud provider configs with potential secrets
.gcloud/
.azure/
terraform.tfstate
terraform.tfstate.backup
*.tfvars

# -----------------------------------------------------------------------------
# DATABASES (May contain PII)
# -----------------------------------------------------------------------------
*.db
*.sqlite
*.sqlite3
*.db-journal
*.db-shm
*.db-wal

# -----------------------------------------------------------------------------
# LOGS (May contain sensitive info)
# -----------------------------------------------------------------------------
*.log
logs/
log/
*.log.*
nohup.out

# -----------------------------------------------------------------------------
# CLAUDE CODE WORKFLOW FILES
# -----------------------------------------------------------------------------

# Backport workflow
.backport-skip

# Claude Code local settings
.claude.local
.claude-session
.claude-session-exit
.claude-exit
.claude-state
.claude-tmp/

# Context recovery files
.claude-checkpoint.md
.claude-recovery-plan.md

# Session exit timestamps
exit.*

# Project-specific Claude instructions (local)
CLAUDE.md

# Claude ignore patterns
.claudeignore

# BTRFS snapshots (used by /test skill)
.snapshots/

# -----------------------------------------------------------------------------
# PYTHON
# -----------------------------------------------------------------------------
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
venv/
.venv/
ENV/
env/
*.egg-info/
dist/
build/
*.egg
.eggs/
pip-log.txt
pip-delete-this-directory.txt

# -----------------------------------------------------------------------------
# NODE.JS
# -----------------------------------------------------------------------------
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.npm/
.yarn/
package-lock.json
yarn.lock
.pnpm-store/

# -----------------------------------------------------------------------------
# IDE & EDITORS
# -----------------------------------------------------------------------------
.idea/
.vscode/
*.swp
*.swo
*~
*.bak
*.orig
.project
.settings/
*.sublime-project
*.sublime-workspace
.spyderproject
.spyproject

# -----------------------------------------------------------------------------
# OS GENERATED
# -----------------------------------------------------------------------------
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
desktop.ini

# -----------------------------------------------------------------------------
# TESTING & COVERAGE
# -----------------------------------------------------------------------------
.coverage
.coverage.*
coverage.json
coverage.xml
htmlcov/
.pytest_cache/
.tox/
.nox/
.hypothesis/
nosetests.xml
test-results/
junit*.xml

# -----------------------------------------------------------------------------
# TEMPORARY & CACHE
# -----------------------------------------------------------------------------
*.tmp
*.temp
.cache/
.temp/
tmp/
temp/
*.cache

# -----------------------------------------------------------------------------
# BUILD ARTIFACTS
# -----------------------------------------------------------------------------
*.o
*.a
*.lib
*.dll
*.dylib
*.exe
*.out
*.app
target/
out/

# Local tool configs
.bandit
pyproject.toml
