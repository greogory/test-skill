{
  "description": "Test-skill automation hooks for testing reminders",
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "prompt",
            "prompt": "Before completing this task, consider: Have tests been run? Use `/test --phase=2` for a quick test run, or `/test --phase=0-3` for discovery through reporting."
          }
        ]
      }
    ]
  }
}
